# Built-in Summary Userscripts

This directory contains the extension's built-in Summary site userscript bodies, one file per site.

- Source bundle: `Mod/assets/chunk-7dbf4e81.js`
- Source symbol: `SUMMARY_SITE_CONFIG_DEFAULTS`
- Source config version: `64`
- Extracted at: `2026-05-23T16:05:39.670Z`

These files are userscript bodies for Simple Chat Hub's Summary bridge. They expect the injected `api` helper object and may use top-level `return` / `await`; they are not standalone Tampermonkey scripts.

See `index.json` for hosts, path prefixes, run mode, timeout, and the matching script file for each site.
